<h3><%= @survey.name %></h3>
<p><%= @survey.description %></p>
<a id="new_question" href="/survey/<%= @survey.id %>/questions/new">Create a new question</a>
<script>
var qmcon = new ModalController({view: ModalView, button: '#new_question', container: "#modal"});
qmcon.attachEvents();
</script>
